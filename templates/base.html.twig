<!DOCTYPE html>
<html lang="{{ item.metadata.lang }}">
    <head>
        <meta charset="UTF-8">
        {% block head %}{% endblock %}
    </head>
    <body>
        <header>
            <div>Marcus Kober</div>
            {% if navigation is not empty %}
            <div>
                <navigation>
                    <ul>
                        {% for element in navigation %}
                            <li><a href="{{ base_path }}{{ element.slug }}">{{ element.navigationTitle }}</a></li>
                        {% endfor %}
                    </ul>
                </navigation>
            </div>
            {% endif %}
        </header>
        <nav>
        </nav>
        <main>
            {% block body %}{% endblock %}
        </main>
        <footer>
            <hr>
            <p><a href="">GitHub</a> — <a href="">LinkedIn</a></p>
            <p>Marcus Kober - <a href="{{ base_path }}impressum.html">Impressum</a> | <a href="{{ base_path }}datenschutz.html">Datenschutz</a></p>
        </footer>
    </body>
</html>
